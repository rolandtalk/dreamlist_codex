[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python run_web.py"
healthcheckPath = "/api/runs/latest"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
